import { type RecurringRule } from '../schema';

export async function getRecurringRules(userId: string): Promise<RecurringRule[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all user's active recurring transaction rules.
    // It should include account and category information for each rule.
    return Promise.resolve([]);
}